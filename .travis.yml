language: objective-c
before_install:
    - curl -O http://archives.oclint.org/releases/0.7/oclint-0.7-x86_64-apple-darwin-10.tar.gz
    - tar -zxvf oclint-0.7-x86_64-apple-darwin-10.tar.gz
    - OCLINT_HOME=/Users/travis/build/valery-demin/spark-ios-framework/oclint-0.7-x86_64-apple-darwin-10
    - PATH=$OCLINT_HOME/bin:$PATH
script:
    - xcodebuild -workspace Framework/SparkFramework.xcworkspace -scheme SparkCore build test -sdk iphonesimulator | tee xcodebuild.log
    - oclint-xcodebuild
    - oclint-json-compilation-database -- -rc=LONG_LINE=200 -rc=LONG_VARIABLE_NAME=50 -max-priority-2 20 -max-priority-3 65
notifications:
  email:
    - Valery_Demin@epam.com